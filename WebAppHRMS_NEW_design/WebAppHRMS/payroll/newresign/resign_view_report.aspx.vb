Imports system.data
Imports System.Data.OracleClient
Imports CrystalDecisions.Shared
Imports CrystalDecisions.CrystalReports.Engine

Partial Class new_resign_view_report_ad17c2ae7566
    Inherits System.Web.UI.Page
    Dim oh As New Helper.Oracle.OracleHelper
    Dim report As New ReportDocument
    Dim fdt, tdt, emp, sql, sql1 As String
    Dim export As New IO.MemoryStream
    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        fdt = Request.QueryString.Get("fdt")
        tdt = Request.QueryString.Get("tdt")
        Dim dt As DataTable

        Dim user() As String = Session("user_id").ToString.Split("!")
        Dim firm As Integer
        firm = Session("firm_id")
        Dim dhead As String


        '--------------- ReqID 8592 starts------------------------------
        If Session("firm_id") = 8 Then
            '---------------------end-------------------------------------
            'fff


            dhead = oh.ExecuteDataSet("select d.dep_head from department_mst d,employee_master t where t.department_id=d.dep_id and t.emp_code=" & user(0) & "").Tables(0).Rows(0)(0)
            If user(0) = dhead Then

                'If acc = 33 Then
                '    dt = oh.ExecuteDataSet("select e.emp_code,m.emp_name,to_date(e.enter_dt) as Apply_dt,to_date(e.resign_dt) as Resign_dt,to_date(e.relieve_dt) as Relieve_dt,case when e.reason=1 then hs.college_nm||' , '||hs.course||' , '||hs.durtion else case when e.reason=2 then pr.reason else case when e.reason=3 then oe.firm||' , '||oe.reason||' , '||oe.nature_work||' , '||oe.salary else case when e.reason=4 then mr.partner_name||' , '||mr.job_partner||' , '||mr.place_marriage else case when e.reason=5 then rr.reason end end end end end  ,case when e.status=0 then 'Applied' else case when e.status=1 then 'Approved' else case when e.status=2 then 'Resigned' else case when e.status=3 then 'Cancelled' else case when e.status=5 then 'Processed' end end end end end as status  ,e.approved_by||'--'||e1.emp_name as approved_by,to_date(e.approve_dt) as approve_dt,e.cancel_by||'--'||e2.emp_name as cancel_by,to_date(e.cancel_dt) as Cancel_dt from employee_master m,m_resign_appl e left outer join  resign_higherstudies_reason hs on (hs.emp_code=e.emp_code and e.status=hs.status ) left outer join resign_personal_reason pr on (pr.emp_code=e.emp_code and e.status=pr.status ) left outer join resign_otheremploy_reason oe on (oe.emp_code=e.emp_code and e.status=oe.status ) left outer join resign_marriage_reason mr on (mr.emp_code=e.emp_code and e.status=mr.status ) left outer join resign_other_reason rr on (rr.emp_code=e.emp_code and e.status=rr.status )    left outer join employee_master e1 on (e.approved_by=e1.emp_code) left outer join employee_master e2 on (e.cancel_by=e2.emp_code) where e.emp_code=m.emp_code and e.status in (0,1,2,3,5) and  to_date(e.resign_dt) between to_date(" & fdt & ") and to_date(" & tdt & ") ").Tables(0)
                'Else
                'dt = oh.ExecuteDataSet("select e.emp_code,m.emp_name,to_date(e.enter_dt) as Apply_dt,to_date(e.resign_dt) as Resign_dt,to_date(e.relieve_dt) as Relieve_dt,case when e.reason=1 then hs.college_nm||' , '||hs.course||' , '||hs.durtion else case when e.reason=2 then pr.reason else case when e.reason=3 then oe.firm||' , '||oe.reason||' , '||oe.nature_work||' , '||oe.salary else case when e.reason=4 then mr.partner_name||' , '||mr.job_partner||' , '||mr.place_marriage else case when e.reason=5 then rr.reason end end end end end  ,case when e.status=0 then 'Applied' else case when e.status=1 then 'Approved' else case when e.status=2 then 'Resigned' else case when e.status=3 then 'Cancelled' end end end end as status  ,e.approved_by||'--'||e1.emp_name as approved_by,to_date(e.approve_dt) as approve_dt,e.cancel_by||'--'||e2.emp_name as cancel_by,to_date(e.cancel_dt) as Cancel_dt from employee_master m,m_resign_appl e left outer join  resign_higherstudies_reason hs on (hs.emp_code=e.emp_code and e.status=hs.status ) left outer join resign_personal_reason pr on (pr.emp_code=e.emp_code and e.status=pr.status ) left outer join resign_otheremploy_reason oe on (oe.emp_code=e.emp_code and e.status=oe.status ) left outer join resign_marriage_reason mr on (mr.emp_code=e.emp_code and e.status=mr.status ) left outer join resign_other_reason rr on (rr.emp_code=e.emp_code and e.status=rr.status )    left outer join employee_master e1 on (e.approved_by=e1.emp_code) left outer join employee_master e2 on (e.cancel_by=e2.emp_code) where e.emp_code=m.emp_code and e.status in (0,1,2,3) and e.emp_code= " & Me.Session("user_id").ToString.Split("!")(0) & " and  to_date(e.resign_dt) between to_date(" & fdt & ") and to_date(" & tdt & ") ").Tables(0)
                dt = oh.ExecuteDataSet("select e.emp_code as ecode,        m.emp_name as ename,        case          when br.branch_name = 'A.O.VALAPAD' then           dm.dep_name          else           br.branch_name        end as br,        case          when br.branch_name = 'A.O.VALAPAD' then           br.branch_name          else           br.area_name        end as area,        m.join_dt as   join, to_date(e.enter_dt) as Apply, to_date(e.resign_dt) as Resign, to_date(e.relieve_dt) as Relieve, round((e.resign_dt - m.join_dt) / 30) as exp, case when e.reason = 1 then hs.college_nm || ' , ' || hs.course || ' , ' || hs.durtion else case when e.reason = 2 then pr.reason else case when e.reason = 3 then oe.firm || ' , ' || oe.reason || ' , ' || oe.nature_work || ' , ' || oe.salary else case when e.reason = 4 then mr.partner_name || ' , ' || mr.job_partner || ' , ' || mr.place_marriage else case when e.reason = 5 then rr.reason end end end end end as reason, case when e.status = 0 then 'Applied' else case when e.status = 1 then 'Approved' else case when e.status = 2 then 'Resigned' else case when e.status = 3 then 'Cancelled' else case when e.status = 5 then 'Processed' else case when e.status = 7 then 'TL/Manager Recommended' else case when e.status = 8 then 'Department Head Recommended' end end end end end end end as status, e.approved_by || '--' || e1.emp_name as app_by, to_date(e.approve_dt) as appr_dt, e.cancel_by || '--' || e2.emp_name as cancel_by, to_date(e.cancel_dt) as Cancel_dt, sm.state_name as state, des.designation as design, qm.qualification as qual, round(x.exp / 30) as ho   from branch_dtl_new br,        department_mst dm,        designation_master des,        employee_master_dtl emd,        qualification_master qm,        employ_personal_dtl ep,        post_master pm,        district_master dms,        state_master sm,        (select x.ecod, sum(x.exp1) as exp           from (select ed.emp_code as ecod, ed.to_dt - ed.from_dt as exp1                   from employ_transfer_dtl ed,                        employee_master     m1,                        employ_firm         ef                  where ed.emp_code = m1.emp_code                    and ed.branch_id = 0                    and m1.emp_code = ef.emp_code                    and ef.firm_id = 8) x,                employee_master m2          where x.ecod = m2.emp_code          group by x.ecod) x,        employee_master m,        m_resign_appl e   left outer join resign_higherstudies_reason hs on (hs.emp_code =                                                             e.emp_code and                                                             e.status =                                                             hs.status)   left outer join resign_personal_reason pr on (pr.emp_code =                                                        e.emp_code and                                                        e.status =                                                        pr.status)   left outer join resign_otheremploy_reason oe on (oe.emp_code =                                                           e.emp_code and                                                           e.status =                                                           oe.status)   left outer join resign_marriage_reason mr on (mr.emp_code =                                                        e.emp_code and                                                        e.status =                                                        mr.status)   left outer join resign_other_reason rr on (rr.emp_code =                                                     e.emp_code and                                                     e.status = rr.status)   left outer join employee_master e1 on (e.approved_by =                                                 e1.emp_code)   left outer join employee_master e2 on (e.cancel_by = e2.emp_code)  where e.emp_code = m.emp_code    and m.branch_id = br.branch_id    and m.emp_code = ep.emp_code    and ep.perm_pin = pm.sr_number    and pm.district_id = dms.district_id    and dms.state_id = sm.state_id    and m.department_id = dm.dep_id    and m.designation_id = des.designation_id    and m.emp_code = emd.emp_code    and emd.qualification_id = qm.qualification_id    and e.status in (0, 1, 2, 3, 5, 7, 8) and to_date(e.resign_dt) between to_date( " & fdt & ") and      to_date( " & tdt & ")    and x.ecod=m.emp_code     ").Tables(0)
                'End If
            Else
                Dim hr As String
                hr = oh.ExecuteDataSet("select t.access_id from employee_master t where t.emp_code=" & user(0) & "").Tables(0).Rows(0)(0)
                If hr = 33 Then
                    dt = oh.ExecuteDataSet("select e.emp_code as ecode,        m.emp_name as ename,        case          when br.branch_name = 'A.O.VALAPAD' then           dm.dep_name          else           br.branch_name        end as br,        case          when br.branch_name = 'A.O.VALAPAD' then           br.branch_name          else           br.area_name        end as area,        m.join_dt as   join, to_date(e.enter_dt) as Apply, to_date(e.resign_dt) as Resign, to_date(e.relieve_dt) as Relieve, round((e.resign_dt - m.join_dt) / 30) as exp, case when e.reason = 1 then hs.college_nm || ' , ' || hs.course || ' , ' || hs.durtion else case when e.reason = 2 then pr.reason else case when e.reason = 3 then oe.firm || ' , ' || oe.reason || ' , ' || oe.nature_work || ' , ' || oe.salary else case when e.reason = 4 then mr.partner_name || ' , ' || mr.job_partner || ' , ' || mr.place_marriage else case when e.reason = 5 then rr.reason end end end end end as reason, case when e.status = 0 then 'Applied' else case when e.status = 1 then 'Approved' else case when e.status = 2 then 'Resigned' else case when e.status = 3 then 'Cancelled' else case when e.status = 5 then 'Processed' else case when e.status = 7 then 'TL/Manager Recommended' else case when e.status = 8 then 'Department Head Recommended' end end end end end end end as status, e.approved_by || '--' || e1.emp_name as app_by, to_date(e.approve_dt) as appr_dt, e.cancel_by || '--' || e2.emp_name as cancel_by, to_date(e.cancel_dt) as Cancel_dt, sm.state_name as state, des.designation as design, qm.qualification as qual, round(x.exp / 30) as ho   from branch_dtl_new br,        department_mst dm,        designation_master des,        employee_master_dtl emd,        qualification_master qm,        employ_personal_dtl ep,        post_master pm,        district_master dms,        state_master sm,        (select x.ecod, sum(x.exp1) as exp           from (select ed.emp_code as ecod, ed.to_dt - ed.from_dt as exp1                   from employ_transfer_dtl ed,                        employee_master     m1,                        employ_firm         ef                  where ed.emp_code = m1.emp_code                    and ed.branch_id = 0                    and m1.emp_code = ef.emp_code                    and ef.firm_id = 8) x,                employee_master m2          where x.ecod = m2.emp_code          group by x.ecod) x,        employee_master m,        m_resign_appl e   left outer join resign_higherstudies_reason hs on (hs.emp_code =                                                     e.emp_code and                                                     e.status = hs.status)   left outer join resign_personal_reason pr on (pr.emp_code = e.emp_code and                                                e.status = pr.status)   left outer join resign_otheremploy_reason oe on (oe.emp_code = e.emp_code and                                                   e.status = oe.status)   left outer join resign_marriage_reason mr on (mr.emp_code = e.emp_code and                                                e.status = mr.status)   left outer join resign_other_reason rr on (rr.emp_code = e.emp_code and                                             e.status = rr.status)   left outer join employee_master e1 on (e.approved_by = e1.emp_code)   left outer join employee_master e2 on (e.cancel_by = e2.emp_code)  where e.emp_code = m.emp_code    and m.branch_id = br.branch_id    and m.emp_code = ep.emp_code    and ep.perm_pin = pm.sr_number    and pm.district_id = dms.district_id    and dms.state_id = sm.state_id    and m.department_id = dm.dep_id    and m.designation_id = des.designation_id    and m.emp_code = emd.emp_code    and emd.qualification_id = qm.qualification_id    and e.status in (0, 1, 2, 3, 5,7,8)  and to_date(e.resign_dt) between to_date( " & fdt & ") and to_date( " & tdt & ") and x.ecod=m.emp_code").Tables(0)
                Else

                    dt = oh.ExecuteDataSet("select e.emp_code as ecode,          m.emp_name as ename,          case            when br.branch_name = 'A.O.VALAPAD' then             dm.dep_name            else             br.branch_name          end as br,          case            when br.branch_name = 'A.O.VALAPAD' then             br.branch_name            else             br.area_name          end as area,          m.join_dt as           join, to_date(e.enter_dt) as Apply, to_date(e.resign_dt) as Resign, to_date(e.relieve_dt) as Relieve, round((e.resign_dt - m.join_dt) / 30) as exp, case when e.reason = 1 then hs.college_nm || ' , ' || hs.course || ' , ' || hs.durtion else case when e.reason = 2 then pr.reason else case when e.reason = 3 then oe.firm || ' , ' || oe.reason || ' , ' || oe.nature_work || ' , ' || oe.salary else case when e.reason = 4 then mr.partner_name || ' , ' || mr.job_partner || ' , ' || mr.place_marriage else case when e.reason = 5 then rr.reason end end end end end as reason, case when e.status = 0 then 'Applied' else case when e.status = 1 then 'Approved' else case when e.status = 2 then 'Resigned' else case when e.status = 3 then 'Cancelled' else case when e.status = 5 then 'Processed' else case when e.status = 7 then 'TL/Manager Recommended' else case when e.status = 8 then 'Department Head Recommended' end end end end end end end as status, e.approved_by || '--' || e1.emp_name as app_by, to_date(e.approve_dt) as appr_dt, e.cancel_by || '--' || e2.emp_name as cancel_by, to_date(e.cancel_dt) as Cancel_dt, sm.state_name as state, des.designation as design, qm.qualification as qual, round(x.exp / 30) as ho           from branch_dtl_new br,          department_mst dm,          designation_master des,          employee_master_dtl emd,          qualification_master qm,          employ_personal_dtl ep,          post_master pm,          district_master dms,          state_master sm,          (select x.ecod, sum(x.exp1) as exp             from (select ed.emp_code as ecod,                          ed.to_dt - ed.from_dt as exp1                     from employ_transfer_dtl ed,                          employee_master     m1,                          employ_firm         ef                    where ed.emp_code = m1.emp_code                      and ed.branch_id = 0                      and m1.emp_code = ef.emp_code                      and ef.firm_id = 8) x,                  employee_master m2            where x.ecod = m2.emp_code            group by x.ecod) x,          employee_master m,          m_resign_appl e           left outer join resign_higherstudies_reason hs on (hs.emp_code =                                                             e.emp_code and                                                             e.status =                                                             hs.status)           left outer join resign_personal_reason pr on (pr.emp_code =                                                        e.emp_code and                                                        e.status =                                                        pr.status)           left outer join resign_otheremploy_reason oe on (oe.emp_code =                                                           e.emp_code and                                                           e.status =                                                           oe.status)           left outer join resign_marriage_reason mr on (mr.emp_code =                                                        e.emp_code and                                                        e.status =                                                        mr.status)           left outer join resign_other_reason rr on (rr.emp_code =                                                     e.emp_code and                                                     e.status = rr.status)           left outer join employee_master e1 on (e.approved_by =                                                 e1.emp_code)           left outer join employee_master e2 on (e.cancel_by = e2.emp_code)          where e.emp_code = m.emp_code            and e.emp_code = 100428            and m.branch_id = br.branch_id            and m.emp_code = ep.emp_code            and ep.perm_pin = pm.sr_number            and pm.district_id = dms.district_id            and dms.state_id = sm.state_id            and m.department_id = dm.dep_id            and m.designation_id = des.designation_id            and m.emp_code = emd.emp_code            and emd.qualification_id = qm.qualification_id            and e.status in (0, 1, 2, 3, 5,7,8) and to_date(e.resign_dt) between to_date( " & fdt & ") and                  to_date( " & tdt & ")               and x.ecod=m.emp_code     ").Tables(0)

                End If
            End If

            '--------------- ReqID 8592 starts------------------------------
        Else
            dt = oh.ExecuteDataSet("select e.emp_code as ecode,       m.emp_name as ename,       case         when br.branch_name = 'A.O.VALAPAD' then         dm.dep_name         else          br.branch_name       end as br,       case         when br.branch_name = 'A.O.VALAPAD' then          br.branch_name         else          br.area_name       end as area,       m.join_dt as join,       to_date(e.enter_dt) as appli,       to_date(e.resign_dt) as Resign,       to_char(e.relieve_dt) as Relieve,       round((e.resign_dt - m.join_dt) / 30) as exp,       case         when e.reason = 1 then          hs.college_nm || ' , ' || hs.course || ' , ' || hs.durtion         else          case         when e.reason = 2 then          pr.reason         else          case         when e.reason = 3 then          oe.firm || ' , ' || oe.reason || ' , ' || oe.nature_work || ' , ' ||          oe.salary         else          case         when e.reason = 4 then          mr.partner_name || ' , ' || mr.job_partner || ' , ' ||          mr.place_marriage         else          case         when e.reason = 5 then          rr.reason       end end end end end as reason,       case         when e.status = 0 then          'Applied'         else          case         when e.status = 1 then          'Approved'         else          case         when e.status = 2 then          'Resigned'         else          case         when e.status = 3 then          'Cancelled'         else          case         when e.status = 5 then          'Processed'       end end end end end as status,       e.approved_by || '--' || e1.emp_name as app_by,       to_char(e.approve_dt) as appr_dt,       e.cancel_by || '--' || e2.emp_name as cancel_by,       to_char(e.cancel_dt) as Cancel_dt,       sm.state_name as state,       des.designation as design,       qm.qualification as qual,       round(x.exp /30) as ho       from branch_dtl_new       br,       department_mst       dm,       designation_master   des ,       employee_master_dtl  emd,       qualification_master qm,       employ_personal_dtl  ep,      post_master          pm,       district_master      dms,       state_master         sm,       (select x.ecod,sum(x.exp1) as exp                  from (select ed.emp_code as ecod,ed.to_dt -ed.from_dt as exp1                          from employ_transfer_dtl ed, employee_master m1,employ_firm ef                        where ed.emp_code = m1.emp_code                           and ed.branch_id = 0 and m1.emp_code=ef.emp_code   and ef.firm_id=" & Session("firm_id") & ")x,employee_master m2                           where x.ecod=m2.emp_code                          group by x.ecod)x,       employee_master      m,              m_resign_appl        e               left outer join resign_higherstudies_reason hs on (hs.emp_code =                                                   e.emp_code and                                                    e.status = hs.status)  left outer join resign_personal_reason pr on (pr.emp_code = e.emp_code and                                              e.status = pr.status)  left outer join resign_otheremploy_reason oe on (oe.emp_code = e.emp_code and                                                  e.status = oe.status)  left outer join resign_marriage_reason mr on (mr.emp_code = e.emp_code and                                              e.status = mr.status)  left outer join resign_other_reason rr on (rr.emp_code = e.emp_code and                                           e.status = rr.status)            left outer join employee_master e1 on                 (e.approved_by = e1.emp_code)                            left outer join employee_master e2 on                           (e.cancel_by = e2.emp_code) where e.emp_code = m.emp_code                                                                  and m.branch_id = br.branch_id          and m.emp_code = ep.emp_code                                 and ep.perm_pin = pm.sr_number  and                       pm.district_id = dms.district_id                   and dms.state_id = sm.state_id                  and m.department_id =dm.dep_id      and m.designation_id = des.designation_id   and m.emp_code = emd.emp_code               and emd.qualification_id = qm.qualification_id                  and e.status in (0, 1, 2, 3, 5)                 and to_date(e.resign_dt) between to_date( " & fdt & ") and                  to_date( " & tdt & ")               and x.ecod=m.emp_code     ").Tables(0)
        End If

        '---------------------end-------------------------------------

        If Session("firm_id") = 2 Then
            dt = oh.ExecuteDataSet("select to_char(e.emp_code) as ecode, m.emp_name as ename, case when br.branch_name = 'A.O.VALAPAD' then dm.dep_name else br.branch_name end as br, case when br.branch_name = 'A.O.VALAPAD' then br.branch_name else br.area_name end as area, to_char(m.join_dt) as join, to_char(e.enter_dt) as Appli, to_char(e.resign_dt) as Resign, to_char(e.relieve_dt) as Relieve, round((e.resign_dt - m.join_dt) / 30) as exp, case when e.reason = 1 then hs.college_nm || ' , ' || hs.course || ' , ' || hs.durtion else case when e.reason = 2 then pr.reason else case when e.reason = 3 then oe.firm || ' , ' || oe.reason || ' , ' || oe.nature_work || ' , ' || oe.salary else case when e.reason = 4 then mr.partner_name || ' , ' || mr.job_partner || ' , ' || mr.place_marriage else case when e.reason = 5 then rr.reason end end end end end as reason, case when e.status = 0 then 'Applied' else case when e.status = 1 then 'Approved' else case when e.status = 2 then 'Resigned' else case when e.status = 3 then 'Cancelled' else case when e.status = 5 then 'Processed' else case when e.status = 6 then 'cancelled by employee after approvel' else case when e.status = 4 then 'Rejected' end end end end end end end as status, e.approved_by || '--' || e1.emp_name as app_by, to_char(e.approve_dt) as appr_dt, e.appr_remark as approver_remark, e.cancel_by || '--' || e2.emp_name as cancel_by, to_char(e.cancel_dt) as Cancel_dt, e.can_remark as cancel_remark,e.rej_rem as rej_rem , sm.state_name as state, des.designation as design, qm.qualification as qual, round(x.exp / 30) as ho from branch_dtl_new br, department_mst dm, designation_master des, employee_master_dtl emd, qualification_master qm, employ_personal_dtl ep, post_master pm, district_master dms, state_master sm, (select x.ecod, sum(x.exp1) as exp from (select ed.emp_code as ecod, ed.to_dt - ed.from_dt as exp1 from employ_transfer_dtl ed, employee_master m1, employ_firm ef where ed.emp_code = m1.emp_code and ed.branch_id = 0 and m1.emp_code = ef.emp_code and ef.firm_id = " & Session("firm_id") & ") x, employee_master m2 where x.ecod = m2.emp_code group by x.ecod) x, employee_master m, m_resign_appl e left outer join resign_higherstudies_reason hs on (hs.emp_code = e.emp_code and e.status = hs.status) left outer join resign_personal_reason pr on (pr.emp_code = e.emp_code and e.status = pr.status) left outer join resign_otheremploy_reason oe on (oe.emp_code = e.emp_code and e.status = oe.status) left outer join resign_marriage_reason mr on (mr.emp_code = e.emp_code and e.status = mr.status) left outer join resign_other_reason rr on (rr.emp_code = e.emp_code and e.status = rr.status) left outer join employee_master e1 on (e.approved_by = e1.emp_code) left outer join employee_master e2 on (e.cancel_by = e2.emp_code) where e.emp_code = m.emp_code and m.branch_id = br.branch_id and m.emp_code = ep.emp_code and ep.perm_pin = pm.sr_number and pm.district_id = dms.district_id and dms.state_id = sm.state_id and m.department_id = dm.dep_id and m.designation_id = des.designation_id and m.emp_code = emd.emp_code and emd.qualification_id = qm.qualification_id and e.status in (0, 1, 2, 3, 5, 6) and (to_char(e.resign_dt) between to_date(" & fdt & ") and to_date(" & tdt & ") or to_date(e.enter_dt) between to_char(" & fdt & ") and to_char(" & tdt & ")) and x.ecod = m.emp_code").Tables(0)
        End If

        report.Load(Server.MapPath("display_resign_report.rpt"), OpenReportMethod.OpenReportByDefault)
        report.SetDataSource(dt)
        report.SetParameterValue("Firm", Session("firm_name"))
        Me.CrystalReportViewer1.ReportSource = report
        'export = report.ExportToStream(CrystalDecisions.Shared.ExportFormatType.Excel)
        'Response.Clear()
        'Response.Buffer = True
        'Response.ContentType = "application/.xlsx"
        'Response.BinaryWrite(export.ToArray())

        'Response.End()

        'Me.CrystalReportViewer1.ReportSource = export

    End Sub

    Protected Sub Page_Unload(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Unload
        report.Dispose()
        report.Close()
    End Sub
End Class

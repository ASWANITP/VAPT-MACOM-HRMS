Imports system.data
Imports System.Data.OracleClient
Imports CrystalDecisions.Shared
Imports CrystalDecisions.CrystalReports.Engine
Partial Class _pl3absent_b889fcdb6028
    Inherits System.Web.UI.Page

    Dim report As New ReportDocument
    Dim dt As New DataTable
    Dim oh As New Helper.Oracle.OracleHelper
    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        If Request.QueryString("cat") = 3 Then
            dt = oh.ExecuteDataSet("select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  bd.AREA_NAME as am,  bd.DIV_NAME as dm,  bd.zonal_name as zm,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  branch_master      c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  branch_detail      bd,  employ_firm         ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NULL  and e_time is NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.branch_id  and a.emp_code = d.emp_code  and d.emp_code=ef.emp_code and ef.firm_id='" & Session("firm_id") & "'  and d.department_id = e.dep_id  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and a.BRANCH_ID = bd.BRANCH_ID  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id union select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  am.area_name as am,  dm.div_name as dm,  zm.zonal_name as am,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  before_completion  c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  area_master        am,  division_master    dm,  zonal_master       zm,  employ_firm        ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NULL  and e_time is NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.old_id  and c.branch_id is null  and a.emp_code = d.emp_code  and d.emp_code = ef.emp_code  and ef.firm_id = '" & Session("firm_id") & "'  and d.department_id = e.dep_id  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and c.area_id = am.area_id  and c.division_id = dm.division_id  and c.zonal_id = zm.zonal_id  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id  order by br, dep, curdt, empid").Tables(0)
        End If
        If Request.QueryString("cat") = 1 Then
            dt = oh.ExecuteDataSet("select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  bd.AREA_NAME as am,  bd.DIV_NAME as dm,  bd.zonal_name as zm,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  branch_master      c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  branch_detail      bd,  employ_firm       ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NULL  and e_time is NOT NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.branch_id  and d.emp_code=ef.emp_code  and ef.firm_id='" & Session("firm_id") & "'  and a.emp_code = d.emp_code  and d.department_id = e.dep_id  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and a.BRANCH_ID = bd.BRANCH_ID  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id union select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  am.area_name as am,  dm.div_name as dm,  zm.zonal_name as am,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  before_completion  c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  area_master        am,  division_master    dm,  zonal_master       zm,  employ_firm ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NULL  and e_time is NOT NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.old_id  and c.branch_id is null  and a.emp_code = d.emp_code  and d.emp_code=ef.emp_code  and ef.firm_id='" & Session("firm_id") & "'  and d.department_id = e.dep_id  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and c.area_id = am.area_id  and c.division_id = dm.division_id  and c.zonal_id = zm.zonal_id  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id  order by br, dep, curdt, empid").Tables(0)
        End If
        If Request.QueryString("cat") = 2 Then
            dt = oh.ExecuteDataSet("select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  bd.AREA_NAME as am,  bd.DIV_NAME as dm,  bd.zonal_name as zm,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  branch_master      c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  branch_detail      bd,  employ_firm ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NOT NULL  and e_time is NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.branch_id  and a.emp_code = d.emp_code  and d.emp_code=ef.emp_code  and ef.firm_id='" & Session("firm_id") & "'  and d.department_id = e.dep_id  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and a.BRANCH_ID = bd.BRANCH_ID  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id union select a.branch_id as br,  c.branch_name,  upper(substr(d.emp_name, 0, 19)) as emp_name,  a.emp_code as empid,  a.curr_date as curdt,  e.dep_name as dep,  am.area_name as am,  dm.div_name as dm,  zm.zonal_name as am,  sm.state_name as sm,  ds.designation,  ps.post_name  from attendance         a,  before_completion  c,  employee_master    d,  department_mst     e,  designation_master ds,  post_mst           ps,  state_master       sm,  area_master        am,  division_master    dm,  zonal_master       zm,  employ_firm        ef  where not exists (select emp_code  from leave_pl3 l  where a.emp_code = l.emp_code  and to_date(l.leave_date) =  '" & Request.QueryString.Get("fdate") & "')  and not exists (select t.emp_code  from training_attend t  where t.emp_code = a.emp_code  and to_date(t.training_date) = to_date(sysdate)  and t.in_time is not null)  and not exists  (select emp_code  from employ_leave_dtl el  where a.emp_code = el.emp_code  and '" & Request.QueryString.Get("fdate") & "' between  to_date(el.leave_frdate) and to_date(el.leave_todate))  and m_time is NOT NULL  and e_time is NULL  and a.shift_id not in (4, 5)  and d.status_id = 1  and a.branch_id = c.old_id  and c.branch_id is null  and a.emp_code = d.emp_code  and d.department_id = e.dep_id  and d.emp_code=ef.emp_code  and ef.firm_id='" & Session("firm_id") & "'  and to_date(a.curr_date) = '" & Request.QueryString.Get("fdate") & "'  and to_char(a.curr_date, 'D') not in  (select h.id from branch_holiday h where h.branch_id = a.branch_id)  and c.area_id = am.area_id  and c.division_id = dm.division_id  and c.zonal_id = zm.zonal_id  and c.state_id = sm.state_id  and d.department_id not in (154)  and d.designation_id = ds.designation_id  and d.post_id = ps.post_id  order by br, dep, curdt, empid").Tables(0)
        End If



        If (dt.Rows.Count = 0 And dt.Rows.Count = 0) Then

            Me.lbl_msg.Text = "NO EMPLOYEES FOUND !!!"

        Else
            Me.lbl_msg.Visible = False
            report.Load(Server.MapPath("pl3absent.rpt"), OpenReportMethod.OpenReportByTempCopy)
            report.SetDataSource(dt)

            Me.CrystalReportViewer1.ReportSource = report
            report.SetParameterValue("Firm", Session("firm_name"))
        End If
    End Sub

    Protected Sub Page_Unload(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Unload
        report.Close()
        report.Dispose()
        GC.Collect()
    End Sub
End Class
